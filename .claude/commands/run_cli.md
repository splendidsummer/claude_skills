---
description: Run the project's CLI application
---

# Run CLI

Run the project's CLI application.

## Steps
1. Check the CLI entry point (usually in `src/demo_app/cli.py`).
2. Identify available commands.
3. Run the CLI with the requested arguments.
4. Report the output.

## Output format
- Command executed
- CLI output/results
- Any errors encountered
